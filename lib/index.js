/**
 * sails-emberjs-responses
 *
 * Pack for parse and respond with a default ember.js response compatible
 *
 */

// SER = sails-emberjs-responses
var SER = require('./sails/hook.js');

module.exports = SER;